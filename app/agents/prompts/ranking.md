Rank products with a 1-sentence explanation. Return STRICT JSON list.

Each product includes 'price' (original) and 'effective_price' (after any applicable coupon) and possibly 'applied_coupon' with a discount code. When effective_price is lower than price, mention the savings in your explanation (e.g. 'originally ₹X, now ₹Y with code Z'). Rank primarily by how well the product matches stated preferences, using effective_price for any price-based comparison, not the original price.
